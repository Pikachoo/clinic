<div id="menu" class="ui menu inverted no-print">
  <%= link_to "Главная", root_path, class: 'item' %>
  <% if current_user %>
      <%= link_to "Личная страница", (current_user.is? 'doctor') ? internal_entry_doctor_path : external_entry_patient_path, class: 'item'%>
  <% end %>
  <%= link_to "Карточка", external_entry_patient_card_path, class: 'item' if can? :read, Card %>
  <%= link_to "Расписание", internal_entry_doctor_receptions_path, class: 'item' if can? :read, DoctorReception %>
  <% if current_user %>
      <%= link_to "Принять внепланового пациента", get_form_unplanned_patient_path, class: 'item' if current_user.is? 'doctor' %>
  <% end %>
  <div class="right menu">
    <% if current_user %>
        <%= link_to "Выход (#{current_user.login})", log_out_path, class: 'item' %>
    <% else %>
        <%= link_to "Войти", log_in_path, class: 'item' %>
    <% end %>
  </div>
</div>
